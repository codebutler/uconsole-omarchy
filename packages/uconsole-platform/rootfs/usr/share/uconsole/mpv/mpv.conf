# Platform defaults; user options after this include take precedence.
hwdec=auto-safe
script=/usr/share/uconsole/mpv/hwdec.lua
# Avoid spending power decoding streams larger than the built-in panel.
ytdl-format=bestvideo[height<=720][vcodec^=avc1]+bestaudio/best[height<=720]
